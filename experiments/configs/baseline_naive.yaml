experiment:
  name: "baseline_naive"
  description: "Naive persistence baseline (predict lag-3 value)"

model:
  type: "naive"
  phase: "naive"

hyperparameters:
  train_window: 90

evaluation:
  method: "cv"
  cv_folds: 3

seed: 42
