experiment:
  name: "xgboost_exog_lags"
  description: "XGBoost with AR features + lagged exogenous ED metrics"

model:
  type: "xgboost"
  phase: "ar_exog_lags"

hyperparameters:
  train_window: 90
  n_estimators: 200
  max_depth: 5
  learning_rate: 0.05
  subsample: 0.8
  colsample_bytree: 0.8
  reg_alpha: 0.0
  reg_lambda: 1.0

evaluation:
  method: "cv"
  cv_folds: 3

seed: 42
