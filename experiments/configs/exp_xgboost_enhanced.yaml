experiment:
  name: "xgboost_enhanced"
  description: "XGBoost with enhanced features (rolling + DOW)"

model:
  type: "xgboost"
  phase: "enhanced"

hyperparameters:
  train_window: 90
  n_estimators: 100
  max_depth: 3
  learning_rate: 0.1
  subsample: 0.8
  colsample_bytree: 0.8
  reg_alpha: 0.0
  reg_lambda: 1.0

evaluation:
  method: "cv"
  cv_folds: 3

seed: 42
